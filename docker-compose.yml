version: '3.8'

services:
  generator:
    build: .
    container_name: generator-app
    ports:
      - "8081:8081"
    environment:
      - JAVA_OPTS=-Xmx512m -Xms256m
      - SPRING_PROFILES_ACTIVE=docker
      - APP_USER_CONFIG_DIR=/app/data/users
      - APP_GLOBAL_CONFIG_FILE=/app/data/default.yml
      - APP_DOWNLOAD_DIR=/app/downloads
    volumes:
      - ./data:/app/data
      - ./downloads:/app/downloads
      - ./output:/app/output
      - ./logs:/app/logs
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8081/v1/gen/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s
    restart: unless-stopped

  # 可选：添加数据库服务用于测试
  mysql:
    image: mysql:8.0
    container_name: generator-mysql
    environment:
      MYSQL_ROOT_PASSWORD: root123
      MYSQL_DATABASE: generator
      MYSQL_USER: generator
      MYSQL_PASSWORD: generator123
    ports:
      - "3306:3306"
    volumes:
      - mysql_data:/var/lib/mysql
      - ./docker/mysql/init.sql:/docker-entrypoint-initdb.d/init.sql
    restart: unless-stopped

volumes:
  mysql_data:
